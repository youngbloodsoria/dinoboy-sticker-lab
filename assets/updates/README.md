Put Brighton update photos here.

Recommended workflow:
- Save web-friendly images as JPG, PNG, or WebP.
- Use simple lowercase filenames with hyphens, like `scan-day.jpeg`.
- Reference them from `data/updates.json` as `assets/updates/scan-day.jpeg`.
- Keep original full-size photos somewhere safe, then use smaller web copies here.

